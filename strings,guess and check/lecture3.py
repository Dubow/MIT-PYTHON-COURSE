#  String Manipulation, Guess and Check, Approximations, Bisection
s = 'abc' #string
print(len(s)) # getting the length of string s
print(s[0]) # getting the position since the index in python starts from 0 and -1 from the last